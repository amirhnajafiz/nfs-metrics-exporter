apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "nfs-metrics-exporter.fullname" . }}-test-connection"
  labels:
    {{- include "nfs-metrics-exporter.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "nfs-metrics-exporter.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
